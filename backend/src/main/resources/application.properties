spring.application.name=xrechnung-validator
server.port=8080

# Raise the multipart limit above the 1MB default so larger invoices don't fail.
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
